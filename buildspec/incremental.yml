version: 0.2

phases:
  install:
    commands:
      - apt-get update -y -q
      - apt-get install -y clang cmake ninja-build -q
      - git clone --depth 1 https://github.com/flutter/flutter.git -b stable
      - export PATH="$PATH:$(pwd)/flutter/bin"
      - flutter config --no-analytics
      - flutter doctor
      - flutter config --enable-web

  pre_build:
    commands:
      - export PATH="$PATH:$(pwd)/flutter/bin"
      - cd incremental
      - flutter pub get
      # Gate the deploy on the analyzer; never mutate source at build time so
      # the deployed artifact is built from the commit as-is.
      - flutter analyze

  build:
    commands:
      - flutter build web --release --pwa-strategy=none --no-wasm-dry-run --dart-define=USER_POOL_ID=$USER_POOL_ID --dart-define=CLIENT_ID=$CLIENT_ID --dart-define=API_DOMAIN=$API_DOMAIN

  post_build:
    commands:
      # Remove empty service worker file (not needed with --pwa-strategy=none)
      - rm -f build/web/flutter_service_worker.js

      # Upload all files with 24-hour cache
      - aws s3 sync build/web s3://$S3_BUCKET/ --delete --cache-control "public, max-age=86400"

      # Invalidate CloudFront cache (all paths)
      - |
        if [ ! -z "$CLOUDFRONT_DISTRIBUTION_ID" ]; then
          aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_DISTRIBUTION_ID --paths "/*"
        else
          echo "No CloudFront distribution ID provided, skipping cache invalidation"
        fi
